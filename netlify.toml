[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# SPA / App Router Fallback Redirect Rule for Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
